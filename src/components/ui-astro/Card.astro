---
import { cn } from "@/lib/utils";

interface Props {
	class?: string;
	[key: string]: any;
}

const { class: className = '', ...rest } = Astro.props;

const classes = cn("rounded-xl border bg-card text-card-foreground shadow", className);
---

<div class={classes} {...rest}>
	<slot />
</div>
