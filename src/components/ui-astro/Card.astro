---
interface Props {
	class?: string;
	[key: string]: any;
}

const { class: className = '', ...rest } = Astro.props;

const classes = `rounded-xl border bg-card text-card-foreground shadow ${className}`.trim();
---

<div class={classes} {...rest}>
	<slot />
</div>
