---
interface Props {
	class?: string;
	[key: string]: any;
}

const { class: className = '', ...rest } = Astro.props;

const classes = `p-6 pt-0 ${className}`.trim();
---

<div class={classes} {...rest}>
	<slot />
</div>
